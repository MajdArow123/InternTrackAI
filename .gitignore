## .NET
bin/
obj/
*.user
*.suo
.vs/

## ASP.NET
# wwwroot/lib/ — vendor files committed so Railway Docker build has them

## SQLite database and WAL files
app.db
*.db-shm
*.db-wal

## User uploads (ephemeral on Railway — use object storage for persistence)
uploads/

## macOS
.DS_Store

## Archives
*.zip

## Logs
*.log

## Secrets — never commit real keys
appsettings.json
secrets.json
appsettings.local.json
